{
  "name": "laboratory-networks",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND -c green,cyan \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "build": "npm run build --workspaces",
    "type-check": "npm run type-check --workspaces",
    "typecheck": "npm run type-check",
    "lint": "npm run lint --workspace frontend",
    "test": "npm run test --workspace backend",
    "check": "npm run type-check --workspaces && npm run lint --workspace frontend && npm run test --workspace backend && npm run build --workspaces"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
